# 更新日志

这里记录玩家、主持人与管理员能够直接感受到的累计变化。较细的开发过程、内部协议调整和测试记录不在本页展开；需要追溯逐版本历史时，请查看[项目仓库](https://github.com/horizoe10/astrbot_plugin_tavern)。

## v1.0.0-rc11 更新

### 🧙 建卡 AI 设定助手

- 建卡私聊新增两条指令：`/团 随机`（别名 sj）由模型为当前字段直接生成一份设定；`/团 补全 <初始设定>`（别名 bq）在保留玩家创意的前提下扩写补全，例如把"一个腐朽的木制魔杖"补全成一套风格统一的遗物装备。
- 生成内容仍走原有字段校验与依赖清理：文本字段会自动控制在字数上限内，职业等预设字段由模型从当前候选中挑选；数值分配与建卡模式选择仍由玩家手动决定。
- 生成失败或校验未通过时角色卡保持不变，已生成内容会原样交还供手动填写；不满意可发送 /团 上一步 后重新生成。

## v1.0.0 累计更新

### 🎲 开团、主持与故事推进

- 串起创建副本、玩家加入、私聊建卡、角色审核、准备、开演、行动选择、检定、投票、暂停、恢复和归档，群里可以从零开始完成一场多人文字跑团。
- 同一个群可以保留不同副本；角色、剧情、任务、记忆和世界状态彼此隔离，不会串到另一场故事里。
- 支持 AI 主持与真人主持。主持权交接时会保留已经确定的投票、检定、行动顺序和故事事实，不需要重新结算当前回合。
- 个人行动、全队行动和关键集体决策都有明确入口。需要投票时先收集意见，再按照最终选项和检定结果推进故事。
- 提供极简、平衡、史诗三档正文模式，分别适合快速推进、日常回合和高潮场景；主持人可以按现场节奏随时调整下一回合的篇幅。
- 增加多对白、偏对白、均衡、偏描写、多描写五档叙事文风，并可填写本场团的具体偏好。文风只影响表达方式，不会改变规则、事实或玩家决定。
- 故事正文支持分段投递和断点续发。内容较长或平台发送中断时，会从安全位置继续，不会把已经发过的段落重复刷屏。
- 生成时间较长时会显示进度提醒；取消、超时、模型限流或质量修复失败后，会保留已经确定的内容并给出可以直接执行的重试或恢复操作。
- 模型偶尔返回中文、带空格或其他不合规的可选语气标签时，系统会忽略该标签并保留已经生成的合格正文，不再因此让整轮故事作废；模型说明也明确了可用格式和示例。
- 修复模型在非对白段落返回空白发言者占位、或在修复故事时遗漏可用角色名单，导致正常正文被判定失败的问题；对白仍必须明确对应当前故事中的真实角色。
- 修复进度提醒写入后执行“重试本轮”偶尔提示事务冲突的问题。重试会复用已锁定的行动、表决和检定，但玩家实际输入发生变化时仍会拒绝错误复用。
- 修复选择第一个行动后偶尔无法继续、失败后只显示模糊错误、普通群聊被误当成跑团行动等问题。

### 🧑‍🤝‍🧑 建卡、角色与队伍

- 私聊建卡支持快速、标准、深度三种模式：新玩家可以尽快开场，熟悉世界的玩家也能逐项塑造完整角色。
- 玩家主动私聊 Bot 后可以自动匹配群内预留席位；角色名、选项序号和其他建卡内容可以直接回复，不必每次都输入完整命令。
- 建卡候选统一使用连续的全局序号，也可以回复完整名称；较长候选会分批发送，但序号不会因分页重新从 1 开始。
- 候选说明会尽量交代用途、优势、限制、适用条件和风险，帮助玩家理解选择，而不是只展示一串名称。
- 修复选择建卡模式后报错、`/团 当前` 无法查看步骤，以及返回上一步、修改字段或重新建卡时提示刷新失败的问题。
- 建卡草稿会保留上次成功状态。无效输入、重复消息或发送中断不会悄悄跳过字段，也不会把半成品提交为正式角色卡。
- 角色卡支持审核、退回修改、补充资料、重新开始和恢复；开演所需资料与可以在剧情中逐步补充的资料会分别处理。
- AI 队友与真人角色显示在同一支小队中，可以参与行动并同步公开状态、资源、背包和能力；模型与内部配置不会显示给普通玩家。
- 角色能力、物品、资金、资源、成长和持续状态会随战役保存，重复点击或网络重试不会重复发放奖励或扣除消耗。
- 建卡与角色提示针对手机阅读重新排版，修复标题层级失衡、选项说明挤在一起和“限制”文字异常放大等问题。

### ⚔️ 挑战、战术与角色命运

- 新增完整挑战流程：主持人从当前世界选择挑战，玩家可以提交行动、退出方案或谈判提案，并在确认前检查和替换自己的草稿。
- 挑战会显示目标、风险、当前进度和失败后的继续方式。失败可以带来代价、时钟或状态变化，但不会因为一段模型文字直接改写规则结果。
- 新增多阶段战术冲突：玩家先声明行动，主持人锁定后再依次结算玩家、敌方、环境和轮末状态，避免边结算边修改造成争议。
- 战术中可以行动、防守、援助、撤退或谈判；目标、区域、能力、装备、资源和当前阶段都会在提交时检查，错误选择会保留草稿并说明如何修正。
- 能力成本、装备数量、耐久、充能、角色资源、元素暴露和环境危机会按世界规则一起结算；其中任何一项冲突时，本次结果都会安全撤回。
- 相同操作在网络重试或响应丢失后会恢复原结果，不会再次掷骰、重复消耗装备或叠加同一个效果。
- 致命后果改为角色本人可见的命运预览。只有角色本人能够确认或拒绝，主持人与管理员不能代替玩家决定角色命运。
- 玩家确认危险后进入可以救援的非终态窗口；普通挑战失败、战术失败或模型叙述不能绕过该流程直接判定角色死亡。

### 🗺️ 世界、冒险内容与创作

- 建立完整世界包体系，世界可以提供角色模板、场景、任务、NPC、阵营、关系、时钟、物品、经济、成长、挑战、战术和结局。
- 进行中的副本会继续使用创建时保存的世界内容；后来安装的世界更新不会在玩家不知情时改写当前战役。
- 内置长期战役《第十三席：万邦新纪》保留七幕主线、61 个场景、38 个原有任务、5 种开局和 5 种结局，并重新梳理线索、阵营动机、失败推进和结局之间的因果关系。
- 《第十三席：万邦新纪》进一步加入 8 个冒险站点、8 个新增任务、28 个挑战、28 个战术冲突、24 个动态危机、20 个幕间事件、24 件远征装备、20 个配方和三档冒险强度。
- 扩充八元素玩法，加入元素反应、相互影响、对象亲和与逐层暴露；元素可以帮助战斗、调查和环境互动，但不能代替证据、承诺、外交程序或角色同意。
- 增加证据保管、承诺与协定、听证与会盟、传闻传播、场景环境等长期玩法，相关变化会真正保存并影响后续故事。
- 《雾港末班车》不再随插件作为内置预设世界安装，方便后续作为独立世界包继续调整；更新插件不会主动删除用户已经导入的世界或由它创建的现有副本。
- 内置世界和单独导入的受检世界包都可提供独立的世界说明和叙事风格。玩家只看到无剧透内容，主持人与管理员可以按身份阅读获准章节。
- 世界库支持检查、预览、安装和更新世界；作者工具支持编辑、构建和检查自制世界，并会在缺少内容、引用失效或文件不安全时给出明确问题。
- 修复世界设定明明已经安装却提示“读取世界设定失败”、世界详情页签消失、内容卡片过长，以及不同世界错误展示同一套玩法的问题。
- 世界库卡片改为有上限的响应式宽度，多张世界会自然并排，不再把单张卡片强制拉满整行；窄屏仍保持完整的一列阅读体验。
- 世界设定按章节、重点提示、列表和资料表重新分组展示，阅读世界包正文时不再把 Markdown 标记和大段原文直接堆在同一块区域。

### 🖥️ WebUI 与日常管理

- WebUI 重建为总览、副本运行、待办与通知、角色审核、世界库、叙事控制、长期记忆、投递审计、健康中心和安全设置等 15 个业务页面。
- 玩家、主持人、作者和管理员会看到与身份相符的页面、数据和操作；普通用户不会接触数据库字段、内部编号、原始配置或调试信息。
- 页面完整区分正在加载、暂无数据、部分读取成功、连接中断、权限不足、内容冲突和服务失败，并提供刷新、重试或返回现场等恢复入口。
- 支持桌面端与移动端、明暗主题、键盘操作、高对比度和减少动态效果；图形信息同时提供可读列表，不依赖颜色或动画才能理解。
- 页面会先显示基础界面，再按需读取当前页面数据；没有打开的世界详情和模型列表不会拖慢其他页面。
- 修复长时间停留控制台后切换页面提示“无法加载动态模块”，以及一个异常模型导致所有页面同时显示连接中断的问题。
- 修复“角色命运”首次打开时可能无法取得 `story.js` 的问题；相关页面组件现在随控制台资源一同加载，不再依赖点击后的临时模块地址。
- 小队角色页按照真人与 AI 队友共用业务卡重新排版：身份、行动状态、公开资源、背包、能力与状态集中展示，完整角色详情增加属性图、资源进度、装备和限制分区，并继续遵守当前世界的可见范围。
- 修复首页 Logo、金色按钮文字、取消按钮边框、弹窗字段对齐、窄屏弹窗遮挡、表单贯穿边线和世界详情布局等视觉问题。
- 编辑弹窗明确区分“预览”和“保存”，设置提交后会重新读取结果；不会再把第一次保存误当成预览或只在页面上假装成功。
- 模型设置直接从 AstrBot 已配置模型中选择，并支持后备模型、图片转述模型、提示词和每回合图片数量；叙事随机度和玩家操作间隔可以正常填写小数。
- 长期记忆与投递审计在宽屏下使用紧凑横向筛选栏，减少大块空白；较窄屏幕会自动换行，不影响触控操作。

### 🛟 数据安全、投递与故障恢复

- 重要操作、迁移和恢复前会建立可核对的备份；恢复前可以先看影响范围，失败时保留原数据，不会把半套结果当成成功。
- 命名存档、自动安全备份、回合恢复点和完整备份共同覆盖角色、故事、任务、时钟、记忆、物品、挑战、战术和角色命运状态。
- 主动私聊、系统通知和较长消息进入可追踪的投递流程；发送失败会保留原因，可在投递与审计页面查看和重试。
- 重复命令、重复点击、平台重发和进程重启后的恢复操作会识别已经完成的结果，避免重复投票、重复通知或重复修改战役数据。
- 错误提示统一说明失败操作、可理解的原因、系统已经自动保留的内容，以及玩家下一步可以执行的命令。
- BOT、WebUI 和世界说明会按观看者身份裁剪内容；玩家消息不会显示稳定内部编号、原始 JSON、模型供应商、服务器路径或错误堆栈。
- 修复留空时限被保存为 `0` 后所有页面无法打开的问题；留空现在按“不限时”处理，真正的断网、配置问题和服务错误也会分别说明。
- 安装内容排除测试资料、缓存、日志、宿主配置和临时文件，降低升级时把开发残留或本机资料带入插件目录的风险。

## v0.1.0—v0.12.0 累计更新

### 🌱 从文字酒馆到多人副本

- 从最初的群聊文字跑团逐步发展为完整多人副本，支持创建、加入、准备、开演、行动、暂停、恢复和结束。
- 建立清晰的副本状态，区分尚未开演、准备大厅、运行中、暂停和已经结束，避免未准备好时提前进入正式回合。
- 支持多个群、多个世界和多个副本相互隔离；每场团分别保存玩家、角色、NPC、故事、记忆、任务、时钟、关系、物品和经济数据。
- 普通群聊默认不会触发跑团模型或写入故事记录；只有明确的跑团命令和当前行动者提交的内容才会进入副本。
- 跑团命令改为 AstrBot 原生指令，修复唤醒前缀被框架处理后命令失效、未知命令落入普通 AI 对话等早期问题。

### 🎲 回合、检定与主持

- 加入多人固定顺序、当前行动权、跳过和下一位等回合管理；非当前玩家的发言不会在上一位结束后被延迟当成行动。
- 玩家行动提供清晰选项，并支持受限的自由补充；故事、世界变化和行动权移交会一起保存，失败时不会丢失当前回合。
- 建立由插件裁定的 D20 检定，统一处理属性、难度、优势与劣势、骰点和结果档位；模型负责讲述结果，不负责偷偷改骰。
- 加入全队行动与集体投票，关键决定可以收集多数意见、进行二轮决选，并保留原行动需要的检定。
- 提供 AI 主持与真人主持两种方式；真人主持可以接管叙事、直接推进、管理 NPC，并在需要时交回自动主持。
- 增加有序后备模型链。主要模型超时、调用失败或连续返回无效结果时，可以按配置尝试后备模型。
- 支持可选图片转述：只处理当前行动中明确提交的图片，先转换成受边界约束的客观描述，再进入故事上下文。

### 🧑‍🤝‍🧑 建卡、席位与玩家生命周期

- 加入群内预留席位、私聊建卡码、分步草稿、角色卡审核和准备状态，让角色资料不必公开堆在群聊中完成。
- 支持世界人数建议、最低开演人数和人数上限，并使用安全的席位占用机制避免多人同时加入时超出上限。
- 角色卡模板可以声明姓名、身份、职业、属性和其他字段；管理页面提供模板预览、结构检查和预算检查。
- 增加候补、暂离、代控、安全退场、封禁和返场流程；玩家离开、停用或删除时会同步维护行动队列。
- 支持个人回合超时、投票计时和全局、世界、当前副本三级时间设置，并提供即时团、慢速群团和全手动等预设。
- 角色运行状态、未完成选项、投票、计时、权限和返场任务都会进入存档，不会只保存一段故事正文。

### 🗺️ 世界、NPC 与长期战役

- 建立世界包导入、编辑、保存和导出流程；副本创建时会保存运行快照，世界后来更新也不会直接热改进行中的故事。
- 世界内容逐步扩展到角色模板、NPC、关系、背包、经济、剧情账本、任务物品、场景时钟、记忆和角色状态。
- 加入世界脉冲事件，可以按照权重、轮次、冷却、唯一性和场景条件在完整回合后推动世界变化。
- 支持动态 NPC、角色托管和主持交接；NPC 的公开资料、关系和当前状态会随副本保存。
- 命名存档、读档、回滚点、定期检查点、安全快照和分支恢复共同覆盖多人轮次与世界状态。
- 完整备份支持安全合并和覆盖恢复；迁移前先建立一致备份，旧的运行中副本会在需要时转为暂停，避免边迁移边继续写入。

### 🖥️ 管理页面、记忆与通知

- 从早期插件管理页逐步扩展出副本仪表盘、现场控制、角色审核、时间与流程、权限封禁、返场、世界编辑和后台管理页面。
- 管理页面支持深浅主题、移动端布局和实时刷新，并逐步统一卡片、弹窗、状态颜色、响应式布局与减少动态效果。
- “长期记忆”从分散卡片改为紧凑列表，集中显示重要度、范围、类型、正文、标签和更新时间。
- 模型设置支持下拉选择、后备顺序、图片模型与健康状态，可查看连续失败、临时熔断和最近错误。
- 主动私聊、提醒和系统通知进入可重试队列；发送失败不会再被误报为成功，可以查看原因、补发或停止重试。
- 增加审计日志、脱敏诊断、自动备份、恢复预览和可选 Webhook，方便管理员排查问题和恢复服务。

### 🛡️ 早期稳定性与安全修复

- 修复首次开团时管理员还未绑定群而陷入初始化死锁的问题；授权管理员可以从真实群事件完成首次绑定。
- 修复主持控制、角色托管、经济调整和危险操作只在页面上限制的问题，服务端也会检查权限并记录操作。
- 修复重复提交、重复投票、重复通知和进程重启后重复产生副作用的问题，关键操作逐步加入持久防重。
- 修复全队投票通过后丢失检定、普通消息误触发故事、主动消息失败误报成功，以及通知无限重排或无法补发等问题。
- 世界包导入增加路径、文件大小、完整性和内容检查，阻止危险路径、损坏文件和异常大成员进入插件数据目录。
- 修复旧数据库迁移中断、恢复只完成一半、平台身份混用、WebUI 空状态误导和移动端操作困难等问题。
