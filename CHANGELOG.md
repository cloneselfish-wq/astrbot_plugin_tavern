# Changelog

## v0.7.0 — 2026-08-01 22:39

> 完成世界包协议 v2 与执行链解耦，并用「阿尔维恩：灰烬王冠」替换原内置预设。

### 世界包与内置内容

- 默认世界标识改为 `aelvion-ashen-crown`，内置故事、角色卡、12 个职业预设、开场选项及 6 名常驻 NPC 均来自阿尔维恩完整世界包。
- 原内置 `border-tavern` 不再存在于默认常量、默认配置或播种流程。
- 阿尔维恩简介重写为王历 742 年、灰月异象、银炬公会与灰烬王冠主线的世界介绍，不再展示程序功能说明。

### 协议 v2

- 增加 `world_schema_version: 2`、能力声明和统一运行契约。
- 数值模式：`none / manual / preset`；裁定模式：`none / narrative / dice_only / attribute`。
- 预设数值读取 `preset_selector`、`bonus_choices` 与 `total_validation`，不再写死 50 / 7 / 3 / 60。
- 属性、职业、危险度使用稳定 ID；职业仍兼容名称和别名输入。
- 建卡字段支持选择、复选、布尔、长文本和派生字段的协议声明。

### 选项与检定

- 危险度不再自动强制检定；检定只由结构化 `check` 与世界能力决定。
- 属性检定严格核对世界属性 ID，未知属性不再按「通用 +0」静默执行。
- 选项正文 50 字限制不再计算插件生成的括号；括号仅显示危险度和检定属性。
- 普通已知后果留在结构数据和后台；致命后果另起警告行。
- 无检定世界清除检定字段并直接推进；兜底四选项全部免检。

### 兼容与修复

- 无协议版本的旧世界继续按旧规则标准化，已有副本仍使用冻结的世界快照。
- 修复手动数值「重填数值」未真正删除旧 `stat_*` 字段的问题。
- 世界包保存时校验协议版本、无数值/属性检定冲突及角色卡模式。

## v0.6.10 — 2026-08-01 17:30

> 按《AI 酒馆职业预设建卡程序修改需求》重构职业预设建卡：改用世界包原生 schema，
> 移除 v0.6.9 的 `bonus_allocation` 私有开关，并把「防篡改」从建议提升为强制校验。

### 变更 · 采用文档原生 schema（不再使用 `bonus_allocation`）

- **触发方式**：世界包在 `rules.character_card.stats` 中声明
  `input_mode = "automatic_profession_base_plus_two_fixed_bonus_choices"`
  **或** `allocation_mode = "profession_base_plus_primary7_secondary3"` 即进入职业预设模式，
  不再需要额外的 `bonus_allocation.enabled` 开关。
- **职业来源**：模板级 `profession_presets`，每项 `base_attributes`（或 `attributes`）
  必须覆盖全部属性且合计 **50**；玩家只需选 `profession` / `primary_attribute` /
  `secondary_attribute` 三项，主属性固定 **+7**、副属性固定 **+3**，最终恒为 **60**。
- **移除**：`database.py` 中的 `match_profession` / `apply_profession_preset` /
  `_apply_bonus_mode` 三个旧方法，及草稿里的 `_bonus_base` / `_bonus_primary` /
  `_bonus_secondary` 字段；改由草稿中的 `profession_base_stats` /
  `resolved_stat_total` 承载。

### 新增 · `tavern/lifecycle.py` 公共函数

- `PROFESSION_PRESET_STAT_MODE`、`uses_profession_preset_stats(template)`
- `find_profession_preset(template, name)`：按 `name` 精确匹配，未命中直接报错并提示重选。
- `attribute_maps(template)`：属性中文名 ⇄ key 双向映射。
- `resolve_profession_stats(template, fields, *, require_complete)`：
  唯一的数值来源。校验基础合计 50、主副属性合法且互不相同、最终合计 60、每项不超模板上下限，
  返回 `base` / `raw` / `labels` / `modifiers` / `primary` / `secondary` /
  `base_total` / `bonus_total` / `effective_total` / `mode`。
- `next_fillable_card_step(...)`：职业模式下自动跳过 `stat_*` 与 `skip_manual_prompt` 字段。
- `repair_profession_preset_draft(...)`：修复升级前的旧草稿，覆盖手填 `stat_*` 并把光标移到首个非属性字段。
- `card_template()`：职业模式下不再生成 10 个 `stat_*` 字段（第 26—35 项彻底消失），
  并在返回值里带上 `profession_presets` / `profession_mode` / `primary_bonus` / `secondary_bonus`。
- `card_stat_allocation()`：职业模式直接返回 `mode = "profession_preset"`、`stat_fields = []`，
  不再进入逐项分配分支。
- `validate_card_template_config()`：职业模式下强制要求三个选择字段齐全、
  至少 1 个职业、每个职业属性齐全且合计 50、且 `最大基础值 + 7 ≤ 属性上限`。

### 新增 · 强制防篡改校验

- `_confirm_card_draft`：确认建卡时用 `resolve_profession_stats` **重新推导**全部数值，
  逐项比对草稿里的 `stat_*`，任一不符即拒绝并提示
  「XX 数值与职业基础属性及主副属性加成不一致」；最终合计不为 60 同样拒绝。
- `/酒馆 审核 <角色>`：审核详情同样重新推导，展示
  职业 / 基础属性 / 主属性 +7 / 副属性 +3 / 最终属性 / 基础总和 / 加成总和 / 最终总和 /
  **校验结果**；存档与公式不符时直接标红为「不通过」并建议驳回。
- `_fill_card_draft`：职业模式下拒绝任何 `stat_*` 手填，提示
  「本世界不使用大项/小项自由分配，请选择主属性+7与副属性+3。」

### 变更 · 交互提示（`main.py`）

- **选职业**：一次性列出全部职业及其 10 项基础数值（含合计 50）与定位说明。
- **选主属性**：显示当前职业与其 50 点基础属性明细，标注「固定+7」。
- **选副属性**：显示已选主属性，明确「不能与主属性相同」，标注「固定+3」。
- **填写完成**：展示职业 / 主副属性 / 最终 10 项属性 / 基础 50 + 加成 10 = 60。
- **预览**：每项显示「最终值（基础 X ± 加成）（检定修正 ±N）」与总和。
- **`/酒馆 重填数值`**：回复改为「【主副属性已重置】职业与职业固定 50 点基础属性均已保留；
  其他角色资料没有改变。」只清主 / 副属性并回到主属性那一步，不动职业与文字资料。

### 兼容性

- 旧的自由分配世界（边境无名酒馆等）完全不受影响：仍逐项填写、仍按总预算校验、
  `/酒馆 重填数值` 仍重填全部属性。
- 升级前已进入第 26—35 项的草稿会在下一条私聊消息时自动修复，无需重新建卡。
- 数据库 schema 未改动，沿用 `character_card_drafts.fields_json` /
  `current_step` / `character_card_versions.stats_json`。

### 验证

- 隔离测试 58 项全通过（对应需求文档第九节测试 1—11）：
  职业列表 12 个各自合计 50、骑士自动赋值、主属性力量→15、副属性意志→9 且合计 60、
  主副相同被拒、非法职业「剑圣」被拒、第 26—35 项不再出现、重填数值只清主副属性、
  力量被篡改为 16 时确认失败、`stats_json` 结构完整、旧自由分配世界仍正常。
- 提示语渲染检查覆盖：职业步骤、主属性步骤、副属性步骤、完成提示、预览、审核详情
  （含篡改场景显示「校验结果：不通过」）。
- `metadata.yaml` 与 `tavern/constants.py` 版本号升至 **v0.6.10**。

## v0.6.9 — 2026-08-01 11:45

> 修复阿尔维恩世界包「职业基础数值被重置为 0」问题，并解除世界包导入的字符长度限制。

### 修复 · 阿尔维恩世界包职业基础数值归零（根因：职业预设位置不匹配）

- **现象**：导入含预设职业（骑士 / 法师 等 12 个）的阿尔维恩世界包后，所有角色数值被强制归零为 0，需玩家手动逐项填写；世界包中已为每个职业预设的基础数值（合计 50）未被套用。
- **根因**：该世界包把职业预设放在 `rules.character_card.profession_presets`（每项含 `attributes` / `base_attributes` 合计 50，外加 `bonus_allocation` 主属性 +7 / 副属性 +3），而旧插件 `apply_profession_preset` 只读 `rules.professions`（此包为空数组），导致 12 个职业一个都匹配不到，基础值从未套用，10 项属性只能从 0 手填。
- **修复（世界包配置驱动，不硬编码）**：新增「固定基础 + 主 / 副属性加成」分配模式，仅在世界包声明 `bonus_allocation.enabled` 时启用，主 / 副字段名、+7 / +3 等参数全部读自世界包：
  - `database.py` 新增 `_apply_bonus_mode`：按中文名匹配 `profession_presets` 职业 → 套用其 `attributes` / `base_attributes` 为基础值（锁定不可手改）→ 主属性 +7、副属性 +3（校验须为 10 属性之一且不与主属性重复）→ 自动算好 10 项 `stat_*` 并跳过手动填数值步骤。
  - `重填数值`：该模式下只清主 / 副属性、恢复职业 50 基础值、回到「主属性」那一步重选（符合世界包 `reset_behavior`）。
  - `main.py`：选职业时列出 12 个职业各自的中文基础数值（此前因读错位置，列表为空）。
  - **向后兼容**：世界包未声明 `bonus_allocation` 时走原逻辑（边境无名酒馆等不受影响）。

### 修复 · 解除世界包导入的字符长度限制

- **现象**：导入 `AI酒馆_v0.6.0_阿尔维恩…第四步职业预设自动数值版_完整世界包.json` 时提示「内容超过 100 字」，导入失败。
- **根因**：角色卡字段 `profession` 的 `label` 长达 557 字（内含 12 个职业清单），而 `card_template()` 对字段 / 属性 `label` 限 100 字、`stat_*` 合成 `label` 限 50 字，触发 `clean_text` 抛错。
- **修复**：放宽全部导入边界，不再对 World 包内容做硬性字数限制：
  - `lifecycle.py` `card_template`：字段 `label` / 属性 `label` / `stat_*` 合成 `label` 上限由 100 / 50 字放宽至 4000 字。
  - `database.py` `_save_world`：世界名 100 → 400、描述 1000 → 20000、世界设定 30000 → 200000、开场镜头 6000 → 50000 字。
  - `pages/console/app.js`：世界名称输入框 `maxlength` 100 → 400，与管理台后端一致。
  - 仅剥离控制字符、保留换行的清洗逻辑不变；玩家填写的姓名 / 代号等身份字段仍受各自 `max_chars` 约束（由世界包或模板决定），不在此放开范围之外。

### 验证

- 模拟「骑士 → 主力量 → 副体质」：力量 15、体质 11、其余 = 基础，合计正好 60；10 个手填字段被自动跳过。
- 阿尔维恩世界包 `validate_card_template_config` 通过；`profession` 557 字 `label` 可正常导入。
- `metadata.yaml` 与 `tavern/constants.py` 版本号升至 **v0.6.9**；打包 45 个文件，zip 顶层结构 `astrbot_plugin_tavern/` 正确。

## v0.6.8.1 — 2026-07-31 17:20

> 完善默认世界「边境无名酒馆」的世界观设定，并去除开场选项的多余约束。

### 内容 · 世界观完善

- **新增 `rules.professions`（预设职业）**：游侠 / 护卫 / 吟游者 / 密探 / 工匠 共 5 个预设职业，
  每个含 `summary`、`good_at`、基础数值 `base_stats`（体魄/敏捷/意志/学识，各职业合计 8 点）
  与 `free_points: 2`（少量可分配数值），以及 `starting_kit` 起始装备与 `note` 定位说明。
  该结构置于 `rules` 之下，可经「导入世界包」完整存活，也由默认世界种子直接写入。
- **去除开场选项的强制约束**：C 选项不再强制「学识」检定，D 选项不再强制「集体行动」——
  两者均为自然的选择项（`requires_check: false`、`collective: false`），由叙事模型按情境决定是否引入检定或协作。

### 建卡流程 · 选卡展示预设 + 职业自动套用（同版追加，版本号不变）

- **选卡即展示全部预设内容**：开始创建角色卡时（`format_card_prompt` 第 1 步）会列出本世界的
  预设职业（名称 + 基础数值 + 可分配点）、主要势力、主要地点；进入「预设职业」字段时再次聚焦职业清单。
- **选定职业自动套用基础数值**：`character_card.fields` 新增可选字段 `profession`；
  当玩家填入任一预设职业（支持中文名 / 英文名 / 含名片段，如「游侠」「ranger」「游侠（向导）」），
  `_fill_card_draft` 会从 `rules.professions` 取该职业的 `base_stats` 自动写入全部数值字段，
  并跳过数值填写步骤直接完成。基础数值合计 8 点，剩余 2 点（`free_points`）可经 `/酒馆 重填数值` 自由分配。
- **优雅降级**：填入的职业若未匹配任何预设，则不套用、不跳过，玩家照常手动分配数值；
  当前世界未配置 `rules.professions` 时该字段只是普通可选文本，不影响其它世界。

### 说明

- 版本号保持 **v0.6.8.1** 不变；本次在原有世界观完善基础上，补上了「选卡展示预设 + 职业自动套用」的建卡流程实现。

## v0.6.8 — 2026-07-31 17:10

> 大幅丰富默认世界「边境无名酒馆」的内容设定（基于 AI 酒馆 v0.6.0 通用世界母模板）。

### 内容 · 默认世界「边境无名酒馆」详化

参考 `AI酒馆_v0.6.0_通用世界母模板.md` 与 `AI酒馆_v0.6.0_世界包空白模板.json`，
将原本只有一句话设定的默认世界扩写为完整可玩的世界包：

- **`system_prompt` 结构化**：补齐【世界定位】【世界常识】【底层规律】【能力与代价】
  【社会与阵营】【身份与知识边界】【因果与连续性】【玩家自主权】【冲突与风险】
  【叙事风格】【硬性禁令】共 11 个标准章节（约 1200 字）。
- **新增 `rules.setting_modules`**：包含 `canon_policy`、`terminology`、`regions`、
  `factions`、`species_and_identities`、`power_systems`、`professions`、`technology`、
  `economy_and_resources`、`travel_and_communication`、`story_spine`、`custom_rules`
  共 12 个子模块，把阵营、职业、地区、货币、裂隙规则等结构化设定交给叙事模型。
- **开场四选一（`opening_choices`）**：改用规范风险值（`safe`/`controlled`），
  C 选项带「学识」标准检定，D 选项标记为集体行动；仍为 A/B/C/D 四选一。
- **事件池（`event_pool`）**：由 2 个扩到 6 个，新增「裂隙低语」「暗巷交易」
  「守望者临门」「旧债上门」，均带 `conditions`、权重与冷却，可被世界脉冲触发。
- **`initial_state` 与 `opening_scene`**：补充 5 条稳定事实、更具体的开场镜头。
- **角色卡（`character_card.fields`）**：显式给出 14 个字段（姓名、代号、外貌、
  背景、性格、目标、信念、羁绊、专长、缺陷、弱点、知识边界、私人秘密、内容边界），
  与空白模板一致，新建角色时即生效。

> 注意：默认世界仅在 `slug=border-tavern` **尚不存在**时由插件首次播种；
> 已存在的世界不会被自动覆盖。要把新设定应用到现有世界，请在管理台
> 「世界与角色 → 导入世界包」粘贴桌面导出的 `边境无名酒馆_v0.6.8_世界包.json`
> （按 slug 更新；如要覆盖旧世界，可先把 JSON 里的 `slug` 改成你现有世界的 slug，
> 或删除旧世界后以 `border-tavern` 重建）。

## v0.6.7 — 2026-07-31 15:52

> 修复 qq_restapi「全量群消息」下所有酒馆指令静默失效的问题。

### 修复 · 指令在 `all_as_at` 模式下完全不触发

**现象**：接入 `astrbot_plugin_qq_restapi` 并把 `full_group_reply.mode`
设为 `all_as_at` 后，群里发送 `酒馆 状态` / `酒馆 开启`，AstrBot 日志显示
事件已进入总线、适配器也打印了「全量群消息命中回复策略」，但机器人毫无
反应，且不产生任何酒馆插件日志。

**根因**（AstrBot 管线时序问题，非本插件配置问题）：

1. AstrBot 的 `WakingCheckStage` 会在**执行任何 handler 之前**，一次性遍历
   全部 handler 求值 filter，把通过者冻结进 `activated_handlers`。
2. `CommandFilter.filter()` 第一行即
   `if not event.is_at_or_wake_command: return False` —— 未唤醒时，全部原生
   指令 handler 在这一步就被剔除。
3. `qq_restapi` 的 `all_as_at` 标记写在
   `@filter.custom_filter(..., priority=-100)` 的 handler 内，运行于
   `StarRequestSubStage`，**晚于**上述冻结时刻。它设置的 `is_wake` /
   `is_at_or_wake_command` 对本次事件的指令匹配已经无效。
4. 本插件原先的裸指令兜底同样要求 `is_at_or_wake_command` 为真，于是群消息
   处理器静默返回 —— 表现为「完全没反应」。

**修复**：新增 `_parse_command_relaxed()`，群聊与私聊入口统一改用它，
**不再依赖唤醒标记**即可识别缺少 `/` 前缀的裸指令：

- `酒馆 状态`、`酒馆 开启`、`酒馆 重整选项`、`酒馆 投票 A` 等 63 个已登记
  动作，无论是否被 @、是否带 `/`，一律正常触发。
- 为避免打扰普通群聊，仅**裸「酒馆」**与**无法识别的动作**（如 `酒馆 你好`）
  仍要求机器人已被唤醒才回应。
- `我们去酒馆吧`、`酒馆真好玩` 等日常用语不会被误判为指令。

**影响范围**：仅放宽指令识别，未改动权限校验、暂停态守卫与任何玩法逻辑；
原有 `/酒馆 xxx` 与 @ 触发方式行为完全不变。

## v0.6.6 — 2026-07-31 15:12

> 修复富卡片「静默空回复」致命缺陷；卡片标题自动跟随当前世界；`重整选项` 补生成提示。

### 修复 · 富卡片发送失败被误判为成功，导致指令“无反应”（严重）

- **现象**：切换到 `qq_restapi` 适配器后，大量 `/酒馆 XXX` 指令看起来“没有触发”——实际上指令已正常执行，但机器人发出的是一条**空消息**。
- **根因**：适配器的 `reply_ark()` / `reply_markdown_aj()` 在平台侧拒绝时**不会抛异常**，只返回空 `message_id`
  （日志佐证：`QQ REST API 发送失败: code=304004, no permission to use this ark template`）。
  旧代码把「调用未抛异常」当作发送成功，直接 `return None` 跳过管线，于是既没有卡片、也没有纯文本。
- **修复**：
  - 改为**校验返回的 `message_id`**：为空即判定发送失败，继续向下降级，最终必定回退纯文本。
  - 新增**熔断机制**：同一档卡片（markdown / ark）连续失败 2 次后自动停用，避免每条消息都白跑一次
    平台请求、浪费被动消息配额并刷屏日志；日志会给出一次性的可操作修复指引。
  - 抽出统一的 `_rich_try_send()`，消除 `_rich_result()` 与 `_send_event_text()` 两处重复实现的不一致。

### 新增 · 卡片标题自动跟随当前世界

- `rich.title` 默认值改为**留空＝自动**：卡片标题自动取当前群正在游玩的
  「副本名 · 世界名」（如 `🍺 边境无名酒馆 · 阿尔维恩：灰烬王冠`），开馆 / 换本后自动更新，无需手工维护。
- 标题在每次命令与自由文本处理时按会话（`unified_msg_origin`）缓存刷新。
- 仍支持手工填写：填写后固定使用该文案，行为与旧版一致。

### 优化 · `重整选项` 补生成提示

- `/酒馆 重整选项` 在调用模型前先回一条
  `🎲 【酒馆】已收到重整请求，正在重新生成本回合选项……`，与 `选择` / `灵感重投` 的交互反馈保持一致。

## v0.6.5 — 2026-07-31 14:38

> 对齐 `astrbot_plugin_qq_restapi` 实测接口，修正富卡片降级链路；新增 `auto` 自动降级模式并设为默认。

### 修复 · 富卡片降级链路不完整

- **问题**：`rich.mode` 为 `markdown` 时，若 QQ 适配器未配置 `markdown_aj_template_id`（AJ 万能模板 ID），`reply_markdown_aj()` 会抛异常。原实现把 markdown 与 ark 两个分支包在**同一个 `try` 块**里，markdown 一旦抛错会直接跳出整个块、**永远不会尝试 Ark**，最终静默退回纯文本——表现为「装了 REST API 适配器但卡片依旧不生效」。
- **修复**：拆分为**逐级独立 `try`**，实现真正的三级级联：Markdown 模板 → Ark 模板 24 → emoji 纯文本。任一级失败只降一级，并各自记录 warning 便于排查。
- 涉及 `main.py` 的 `_send_event_text()` 与 `_rich_result()` 两处发送入口。

### 新增 · `auto` 自动降级模式（新默认值）

- `rich.mode` 新增 `auto` 选项并**取代 `markdown` 成为默认值**：优先 Markdown 通用模板，未配置 AJ 模板 ID 时自动降级 Ark 模板 24（**主动消息免申请**），再不行退回 emoji 纯文本。
- 原因：Ark 模板 23/24/37 在主动消息场景默认开放，而 Markdown 模板需要额外申请 `template_id`。默认 `auto` 可让绝大多数用户装好适配器即见卡片，无需先申请模板。
- `_conf_schema.json` 同步更新选项、中文标签与说明；`tavern/config.py` 取值白名单扩充为 `{auto, markdown, ark, text}`，非法值回落 `auto`。

### 说明 · 卡片直发返回 `None` 的安全性

- 已核对 AstrBot 管线 `StarRequestSubStage`：处理器 `yield` 出的非 `MessageEventResult` 值（含 `None`）仅做透传、不会置入结果，因此「适配器直发卡片 + 返回 `None`」为安全写法，不会产生空消息或异常。

### 接入前置条件（重要）

- 富卡片依赖 **`astrbot_plugin_qq_restapi`** 适配器（提供 `reply_markdown_aj` / `reply_ark`），要求 **AstrBot ≥ v4.26.0**，且需在管理面板将平台从 `qqofficial` 切换为 `qq_restapi` / `qq_restapi_webhook`。
- 未安装该适配器时，本插件行为与 v0.6.3 完全一致（emoji 纯文本），不影响既有部署。

### 文档 · 修正适配器插件名拼写

- 全仓库将误写的 `astrobot_plugin_qq_restapi` 更正为 **`astrbot_plugin_qq_restapi`**（`astrbot`，无多余的 `o`），涉及 `README.md`、`CHANGELOG.md`、`_conf_schema.json` 配置说明与 `main.py` 注释，避免按错误名搜索导致找不到仓库。

## v0.6.4 — 2026-07-31 14:04

> 第二档「真·结构化卡片」：QQ 官方平台下回合选项 / 剧情 / 状态等消息可渲染为 Ark / Markdown 卡片；不支持的适配器自动退回第一档 emoji 纯文本。

### 富卡片（第二档）· 真·结构化卡片
- **新增能力**：QQ 官方平台下的酒馆消息（回合选项、剧情推进、投票、恢复、暂停 / 状态等）现在可渲染为**结构化卡片**，而不只是 emoji 纯文本。
- **实现方式（双轨、零破坏）**：所有消息出口统一经过新增的富卡片发送层（`_rich_result` / `_send_event_text` 富卡片分支）。
  - 当**配置开启**且**当前事件对象支持 `reply_markdown_aj` / `reply_ark`**（由 `astrbot_plugin_qq_restapi` 等 REST API 适配器提供）时，自动发送 Markdown 卡片（通用模板，长文本与排版最稳）或 Ark 模板 24（文本 + 缩略图）。
  - 否则（内置 WebSocket 适配器、或其他不支持富卡片的平台）**自动退回第一档 emoji 纯文本**，行为与旧版完全一致，绝不抛异常、不阻断游戏。
- **配置项**（控制台 `rich` 分组）：`enabled`（默认开）、`mode`（默认 `markdown`，可选 `ark` / `text`）、`title`（卡片标题，默认「🍺 边境无名酒馆」）。
- **前置条件（重要）**：你当前使用的是 AstrBot 内置 **qqofficial(WebSocket) 适配器**，其官方文档列出的可发送类型仅含文本 / 图片 / 语音 / 视频 / 文件，**原生不支持 Ark / Markdown**。要真正看到卡片，需要让机器人事件具备 `reply_markdown_aj` / `reply_ark` 能力——推荐安装 `astrbot_plugin_qq_restapi`（将 QQ 连接切到 REST API 适配器），或确认你的 AstrBot 版本内置已支持；未满足时插件会自动保持 emoji 纯文本，无需回退版本。
- **关于"真·彩色"**：默认模板 23 / 24 / 37 与 Markdown 通用模板均为**平台统一配色的结构化卡片**，不能自由改色；若要完全自定义配色 / 版式，需向 QQ 运营申请自定义模板（更高门槛）。本版以"真·卡片 + 统一配色"落地第二档，覆盖"全部消息卡片化"诉求中的稳定可落地部分。
- **影响范围**：纯增量能力；`rich.enabled=false` 或 `mode=text` 时等价于关闭；其它平台（Discord / 飞书 / 微信等）沿用各自原生渲染，不受影响。

## v0.6.3 — 2026-07-31 13:13

> 「世界与角色」导入入口拆分为独立选项；叙事消息美化补齐恢复流程头部；修复「暂停后仍可重整选项 / 投票 / 行动」的暂停态守卫缺失。

### 世界与角色 · 导入入口拆分为独立选项
- 原「导入 JSON」单一入口（两个 tab 共用字段/文案）拆分为两个完全独立的按钮：「导入世界包」（`import-world-package-button`）与「导入常驻NPC」（`import-npc-button`），二者界面与流程不再雷同。
- 新增 `openWorldPackageImportDialog()` 与 `openNpcImportDialog()`（替换原 `openImportDialog`），各自独立的标题、说明、提交按钮与结果区；保留共享辅助 `_readImportSource`。
- 「导入世界包」按 `slug` 新建或更新世界，**导入成功后自动刷新并直接打开该世界编辑界面**（`openWorldEditor(world)`），给出「世界包已导入并创建世界」/「世界包已更新」的明确反馈。
- 「导入常驻NPC」保持选择目标世界后批量导入、成功后打开角色管理器。
- 后端 `worlds/import` / `characters/import` 逻辑不变。

### 叙事回合消息美化（恢复流程补齐）
- `main.py` 恢复类消息头部补齐 emoji：`📜 【故事继续】`、`🎭 当前行动者：`、`📖 【恢复时的最后剧情】`、`⏳ 【恢复计时】…`，与正常回合的 `🎯/🅰️~🅳️/💬/📝/♻️` 风格统一，整条消息观感一致。
- 未触碰 `【】` 标记文本本身，不影响既有解析与测试。

### 修复 · 暂停态守卫缺失（/酒馆 暂停 后仍可重整选项 / 投票 / 行动）
- **问题**：执行 `/酒馆 暂停` 后，提示「剧情已暂停、暂停期间默认不计时」，但他人发送 `/酒馆 重整选项`（以及 `选择` / `投票` / `灵感` 等）仍会正常执行、重新生成选项并给出新回合，等于在暂停态下把游戏“顶”了回来。
- **根因**：暂停态守卫原本只写在「自由文本消息」处理分支（`_handle_message`，对群内直接叙事情景生效），而 **指令处理分支（`_handle_command`，即所有 `/酒馆 XXX` 命令的入口）没有任何暂停态判断**；引擎侧 `reroll_choices()` 也缺少 `process()` 早已具备的「非 running 态即拒绝」校验。本质上不是权限/优先级配置问题，而是**命令入口缺了一道暂停闸门**。
- **修复**：
  - 在 `_handle_command` 入口新增暂停态守卫：会话处于 `paused` 时，拦截会改变剧情 / 选项 / 投票 / 队列 / 角色卡的玩法指令（`choose` `reroll` `inspiration` `inspiration_reroll` `vote` `join` `ready` `away` `return_queue` `return_request` `delegate` `delegate_revoke` `leave` `order` `skip` `next` `move` `designate` `perform` `force_ready` 以及全部 `card*` 建卡指令），统一返回「【酒馆】剧情已暂停，暂不可进行选项 / 投票 / 行动 / 建卡等玩法操作。请先由主持人发送 /酒馆 恢复 …」。
  - 守卫**不拦截**主持人 / 管理员的会话管理类指令（`恢复` `继续` `关闭` `完结` `强制终止` `存档` `读档` `回滚` `维护` `状态` `倒计时` `延时` `审核` `封禁` 等），其中「恢复」「续演」仍需 host 权限（权限层照旧校验），确保主持人可正常解暂停。
  - 引擎侧 `reroll_choices()` 补充与 `process()` 一致的「`state != "running"` 即抛出 `酒馆当前不在运行状态，无法重整选项`」校验，使引擎自身对暂停 / 维护 / 恢复准备等状态保持幂等、不误改世界状态。
- **影响范围**：仅暂停态（及非 running 态）下的玩法指令被拦截；正常运行、准备、恢复准备大厅等流程不受影响。无配置项变更，向后兼容。

## v0.6.2 — 2026-07-31 12:54

> 「世界与角色」板块新增 JSON 导入入口，并对叙事回合消息做 emoji 分区与留白美化。

### 世界与角色 · JSON 导入入口
- 在「世界与角色」板块工具栏新增「导入 JSON」按钮（`index.html` 的 `import-world-json-button`）。
- 新增导入弹窗 `openImportDialog()`（`pages/console/app.js`）：支持「世界包」与「常驻 NPC」两种类型切换；可用文件选择或直接粘贴文本；前端先 `JSON.parse`，失败时给出明确中文提示（如「JSON 解析失败：…」）；成功后刷新板块，NPC 导入后自动打开角色管理器展示结果。
- 后端新增 `POST worlds/import`：校验 JSON 顶层为对象、`slug/name/system_prompt` 必填、`rules/initial_state` 必须为对象；按 `slug` 新建或更新世界（upsert）。
- 后端新增 `POST characters/import`：校验 `world_id`/`world_slug` 必填、角色数组非空、每项 `name` 必填、`profile` 为对象；批量写入目标世界，按 `name` 幂等更新；slug 自动生成 `npc_<uuid>`。
- 错误统一经 `_handle_error` 返回 400/404/409 并带中文错误信息；已用真实世界包与常驻 NPC 文件做端到端落库验证（世界 upsert 同一 id、6 个 NPC 创建且重复导入仍为 6 条、`private_direction` 保留）。

### 叙事回合消息美化（第一档：emoji 分区 + 留白）
- `tavern/engine.py`：故事推进头部加 📖、检定加 🎲、回合秩序加 ⚔️、世界脉冲加 🌐、集体决策加 🗳️，并保留原 `【】` 标记以兼容既有测试。
- `tavern/lifecycle.py` 的 `format_choices`：行动回合头部加 🎯，选项改用 🅰️🅱️🅲️🅳️ 分行卡片样式，指令行加 💬/📝/♻️。
- `main.py` 的 `_story_reply_parts` 拆分为「故事 + 回合」时兼容 emoji 前缀，避免 ⚔️ 导致拆不开。
- 仅改排版模板，不动核心逻辑与存档/计时行为；`test_vnext.py` 受影响断言已同步更新。

## v0.6.1 — 2026-07-31 12:17

> 角色卡模板预览无响应与计时通知刷屏（QQ 官方频控 `40034100`）修复版。

### 角色卡模板预览（管理台）
- 修复世界编辑器「玩家角色卡模板」右侧「预览建卡流程」按钮点击后无任何响应的问题。
- 根因：点击处理未做异常捕获，模板校验失败时静默失败；且依赖 `window.alert`，在 AstrBot 控制台以 iframe 跨源嵌入时被浏览器静默拦截。
- 改为 `try/catch` + 页面内渲染：校验通过后把结构化预览渲染到模板下方容器，失败则在页面内显示红色错误文案与提示，不再依赖系统弹窗。

### 计时通知刷屏（对应 bug 记录 `40034100`：主动消息超过频控限制）
- 修复后台关闭所有倒计时提示、并执行恢复／继续／读档后，bot 仍持续刷屏发送「酒馆倒计时」消息的问题。
- `_resume_session_timers` 不再无条件复活全部已暂停计时器：先读取 `timer_policies`，策略关闭的计时器保持暂停并打 `paused_by_policy` 标记，不再被唤醒。
- `_create_timer` 新建前先取消同类已激活／已暂停计时器（非单例类型按 `participant_id` 区分），避免继续／读档／回合推进叠加多条重复计时器。
- `_process_due_timers` 分发提醒前再做一次策略校验（`_countdown_allowed` 闭包，带缓存）；策略不允许则把该计时器置为暂停并打 `paused_by_policy`，不再推送。
- `_timer_loop` 由「异常时派生新任务」改为单一自愈循环，异常仅 `sleep` 后原地重试，避免叠加多个并行轮询放大刷屏。
- `_send_timer_notice` 新增去重与频控：按 `会话｜类型｜参与者｜种类` 组合键 25 秒去重，并在每条主动消息之间保持至少 2 秒最小间隔，规避 QQ 官方 `40034100` 频控后整批被拒又反复重试。
- 确认 `_set_timer_policy` 在管理员重新打开倒计时时会正确唤醒带 `paused_by_policy` 标记的计时器，不会被永久卡死。

## v0.6.0

> 首个无预发布后缀的完整版本；整合 Alpha 阶段功能并强化剧情恢复与模型输出容错。

- 修复暂停、读档或回滚后重新准备完成时仍提示 `/酒馆 开演` 的错误；已有剧情统一提示主持人使用 `/酒馆 继续`。
- 拆分恢复状态机命令：`/酒馆 恢复` 仅进入恢复准备大厅，`/酒馆 继续` 仅在全员重新准备后正式续演；误在暂停状态发送“继续”不会切换状态或恢复计时。
- 管理员强制推进的主命令改为 `/酒馆 强制下一位`；建卡提醒与副本倒计时分别统一为 `/酒馆 建卡提醒` 和 `/酒馆 倒计时`，停用容易误解的快捷名称。
- 恢复旧进度时优先读取并原样重放存档现场的最后剧情、活动 A/B/C/D、集体投票和原剩余时间，不调用模型重写，也不使用当前版本的生成规则重新校验旧选项。
- 修复恢复路径在确认已有活动选项之前先生成兜底选项，因地点或现场摘要过长而错误触发“每个行动选项不得超过 50 字”的问题。
- 恢复正式运行时取消准备大厅计时器，续跑暂停的行动／投票计时器；集体投票显示的截止时间会随剩余时间同步刷新。
- 将叙事核心与 `next_choices` 分层解析和校验；故事、状态变化与权威检定合格后，不再因下一组选项局部格式错误而整轮作废。
- 兼容 `选项A`、`A.`、`A、`、全角字母以及四项全部省略标签等无歧义模型输出；缺项、重复、空正文、超长、无安全项、角色串台和越权操控仍保持硬校验。
- 选项不合格时只调用选项专用修复，不重写剧情、世界状态或骰点；修复请求按 `story_choices` / `story_choices_repair` 独立记入 Token 流水。
- 主模型与备用模型均无法生成合法选项时，插件自动绑定下一位角色并使用四个安全兜底选项，正常提交本轮，避免故事卡死。
- 集体投票已合法生成时忽略无关的错误个人选项；两阶段检定的骰点操作回执保持锁定，选项修复不会重新投骰。
- 安全兜底选项对超长地点和场景摘要执行有界裁剪，保证自身始终符合 50 字限制。
- 继承 v0.5.3 Alpha.1 的主动分条发送、群级 Token 入口、角色卡建成群通知、七类倒计时、存档管理、归档读取及 Schema 6 兼容能力。

## v0.5.3-alpha.1

> 回合主动发送与建卡、群配额入口热修版。

- 修复 `jg A/B/C/D`、`/酒馆 选择`、灵感检定选择在先发送“后续内容正在生成中”后，被 AstrBot 结束事件传播、实际叙事生成未启动的问题。
- 生成提示、故事正文、回合秩序与下一位选项改为按顺序主动发送；不再依赖同一事件处理器连续 `yield`，同时保留发送失败时的单条回退。
- 群 Token 限额从故事副本详情移到群会话标题旁的独立入口；副本详情只保留当前副本自己的用量与限额。
- 新增群级 Token 用量与限额管理接口；群限额变更会同步当前群全部故事副本的独立运行库。
- 玩家在私聊确认角色卡后，Bot 会向角色卡绑定的原群发送“角色卡 xxx 已建立，等待审核”；自动审核模板会显示已自动通过。
- 角色卡已经提交成功但群通知发送失败时，仅记录日志，不会误报建卡失败或诱导重复提交。

## v0.5.3-alpha

> 剧情恢复、移动端短篇输出与运行配额管理版。

- 修复暂停、重新开启或读档后错误调用新开局流程、回到模板初始四选项的问题；已有剧情进度只能使用“继续”，并原样恢复活动选项、投票、行动者及剩余时间。
- 存档新增剧情事件锚点与历史分支排除范围；读档保留存档点之前的对话供叙事上下文使用，放弃分支仍可审计但不再进入模型。
- 恢复提示会重新展示存档点最后一段正式剧情、当前多人轮次与活动选项，不调用模型重写现场。
- 回合提交后立即发送“后续内容正在生成中”；生成完成后先单独发送故事，再发送回合秩序与下一位角色选项。
- 故事自然段之间统一插入 `-----------` 分隔符，改善手机端阅读。
- 默认强制每回合故事正文为 100—300 个可见字符；四个行动选项各不超过 50 字（含括号），不合格输出进入受限 JSON 修复或模型回退，绝不提交半截状态。
- 下一组选项绑定权威 `actor_id`；模型只获得其他玩家的公开可观察资料，并通过生成后校验阻止替其他角色说话、移动、用技能、消耗物品或作决定。
- 新增 `/酒馆 倒计时` 总开关与建卡码、角色卡完成、准备大厅、准备确认、行动回合、集体投票、候补等待七类开关；关闭会冻结真实剩余时间并停止超时处罚，恢复后续计。
- 新增 `/酒馆 强制全员准备 确认` 与管理台按钮；只批量处理角色卡已审核通过且当前出场的玩家，不绕过建卡和审核，也不自动开演。
- 同名存档冲突改为明确的覆盖确认流程；聊天使用“覆盖确认”，管理台使用二次确认，成功后返回独立的覆盖成功提示。
- 新增按群、按故事副本的模型 Token 流水、请求类型统计、供应商实报／本地估算标记，以及滚动时间窗限额。
- Token 限额在调用模型前通过数据库事务原子预占，完成后按实际用量结算；模型重试、JSON 修复、检定二阶段、选项重整和图片转述分别计量。
- 管理台新增 Token 用量与群／副本限额设置；聊天新增 `/酒馆 用量` 与 `/酒馆 限额`。
- 修复永久归档详情误走可写校验而无法查看时间线的问题；归档读取长期记忆时不再更新活跃度，保证真正只读。
- 新增普通快速存档删除、独立命名存档 ZIP 回收，以及已关闭／已归档故事副本的整副本删除；整副本删除要求输入完整副本名，并将独立目录移入服务器回收目录。
- 数据库升级至 Schema 6；新增倒计时策略、Token 用量和 Token 配额表，并纳入完整备份、覆盖导入与旧库自动迁移。
- 保留 v0.5.2 的 Windows SQLite 显式关闭、短时文件锁重试与安全原子替换修复。

## v0.5.2-alpha

> 副本选择、聊天审核与 QQBot 建卡体验修正版。

- `/酒馆 开启` 的副本选择列表新增每个副本对应的世界简介；过长简介会在不撑爆群消息的前提下安全截断。
- 副本选择列表改为每页最多 5 个，显示当前页、总页数与副本总数，并提供明确的上一页、下一页命令。
- 新增 `/酒馆 开启 第2页` 翻页格式；`/酒馆 副本列表 2` 与 `/酒馆 副本列表 第2页` 使用同一分页结果。
- 页码超出范围时自动回到最后一页；翻页只读取列表，不会创建、切换或推进副本。
- 为 `建卡、填写、预览、重填数值、确认建卡、取消建卡、审核` 补齐 AstrBot 原生子命令注册。
- 修复 QQBot 私聊中半角 `/酒馆 ...` 被原生命令路由提前拦截、只有全角 `／酒馆 ...` 可用的问题；两种斜线继续同时兼容。
- `/酒馆 审核` 现在直接列出当前副本全部待审核玩家，每页最多 5 人，并显示序号、角色名、代号、玩家名及稳定审核号。
- 新增 `/酒馆 审核 <序号或审核号>` 与 `/酒馆 审核 查看 <序号或审核号>`，可在群聊查看动态模板中的角色资料、完整角色数值、预算及检定修正；私密字段只显示填写状态，避免泄露到群聊。
- 新增 `/酒馆 审核 <序号或审核号> 通过 [备注]` 与 `驳回 [原因]`；原有按角色名或代号审核继续兼容，处理后会提示剩余待审核人数。
- 私聊建卡在进入第一项属性前新增“接下来开始填写角色数值”提示，并持续显示总预算、已使用、剩余点数与当前实际可填范围。
- 属性填写改为逐项执行权威预算校验；前面数值占用预算后，后续数值的实际上限会自动递减，并为尚未填写的属性保留模板规定的最低点数。
- 新增私聊 `/酒馆 重填数值`：确认建卡前只清空角色属性并回到第一项数值，姓名、背景、性格、秘密及其他文字资料全部保留。
- 角色卡预览新增属性值、检定修正、预算使用情况和“重填数值”指令提示。
- 角色卡全部字段禁止空格、全角空格、换行及制表符；填写时即时拒绝，最终确认时会再次完整校验旧草稿。
- 角色姓名与副本代号统一硬限制为最多 12 个字符；默认模板、导入模板、运行时填写与最终确认使用同一上限。
- 修复选择“需检定”行动后，模型未申请检定便导致整轮安全回滚的问题；首阶段现在会收到不可改写的选项检定契约。
- 即使模型错误地直接返回普通裁定，插件也会丢弃该结果，依据选项中已锁定的属性、难度、风险、检定类型与优劣势自动发起权威检定，再生成最终叙事。
- 修复 Windows 下副本实时库同步时，SQLite 临时连接未显式关闭便执行原子替换，触发 `WinError 32` 并导致插件启动失败的问题。
- 副本同步、旧库迁移和完整备份导出现在都会先关闭全部 SQLite 文件句柄；文件被安全软件短暂占用时进行有限重试，同时继续保证正式 `instance.sqlite3` 只在新库完整生成后原子替换。
- 临时库清理改为不遮蔽主异常；即使残留临时文件仍被占用，日志也会保留真正的同步失败原因，现有正式副本库不会被删除。
- 自动化回归增至 91 项，新增聊天审核名单/详情/处理、动态预算上限、数值重填、文字资料保留、空白字符、身份字段长度、必检选项自动恢复及 Windows SQLite 文件锁用例。

## v0.5.1-alpha

> 存储迁移与管理体验验证版；项目名称仍为「AI 酒馆」。

- 数据库升级至 Schema 5；运行目录库改为 `catalog.sqlite3`。
- 新增“一群一目录、一轮副本一目录、一副本一运行库”的持久化布局。
- 副本目录采用“安全故事标识＋十四位创建时间＋不可变副本 ID”；同一故事重新开局会创建新副本 ID 和新目录。
- 每个副本持续生成自包含 `instance.sqlite3`、`manifest.json`、`saves/` 与 `backups/`。
- 群目录新增 `group.json`；目录名使用平台和稳定哈希，不受群备注、中文名或改名影响。
- 回合级撤销点、自动检查点和操作前保护仍保存在副本数据库内部，维持快速恢复。
- 手动命名存档、最终存档和周期安全备份新增独立 ZIP；文件名统一使用 `YYYYMMDDHHMMSS` 十四位数字时间，同秒重名自动追加序号。
- 独立 ZIP 包含 `instance.sqlite3`、`manifest.json`、`group_snapshot.json` 与 `checksum.sha256`。
- 旧 `tavern.sqlite3` 使用 SQLite 原生一致性备份迁移；迁移完成后旧文件转存至 `migration_backups/`，失败则保留原库并停止切换。
- 管理台新增群备注，支持修订冲突检测和 `group.json` 同步。
- 群会话新增服务端检索、范围筛选和分页，可检索群备注、群 ID、平台 ID、副本名称、标识、世界和会话 ID。
- 群会话按群折叠展示，并显示同一世界的第几次游玩、创建时间及副本文件同步状态。
- 世界卡片新增独立“角色卡模板”入口，支持本地 JSON 导入、导出、结构校验、表单预览、恢复默认和确认保存。
- 群会话“准备与角色”改为每名玩家一张可展开的完整角色卡；按当前副本锁定的动态模板显示全部资料字段、私密字段标识、属性预算、检定修正、副本运行状态、版本、审核记录和席位信息。
- 完整角色卡覆盖未建卡、建卡中、待审核、已通过、未通过及归档角色；后台同时读取草稿已填内容和正式角色卡版本，未填写字段也会按模板完整列出。
- 全量备份改为带十四位时间的 ZIP，同时保留 `bundle.json` 以兼容既有安全合并和覆盖导入。
- ZIP 导入会核对全部 SHA-256，拒绝路径穿越、符号链接、重复成员和异常解压体积，并恢复副本目录中的独立存档。
- 修复同一群、同一世界在同一秒内重新开局时，随机副本 ID 排序可能令新副本仍显示为“第 1 次游玩”的问题。
- 修复同一玩家进入第二轮故事时，旧角色卡的历史私聊绑定会阻止新角色建卡的问题；现在只有另一张尚未完成的活动草稿会产生冲突，副本完结时也会关闭遗留草稿。
- 自动化回归增至 81 项，新增 Schema 5、目录隔离、独立数据库校验、时间戳文件、群备注、双范围检索、完整角色卡数据、动态卡片渲染、重复游玩私聊重绑、ZIP 恢复、路径重建及重复开局用例。
- 重整发布文档：README 仅保留项目定位、主要功能和基础使用说明，逐版本新增、调整与修复统一集中到本更新日志。

## v0.5.0-beta

> 项目名称继续使用「AI 酒馆」；产品方向升级为 AI 主持的多人群聊跑团与互动叙事。

- 新增优势 `2D20 取高`、劣势 `2D20 取低`、相互抵消和来源校验。
- 检定升级为五档结果，并把 DC、风险等级与优劣势职责分离。
- 新增领队、集体、独立抵抗和对抗检定；自然 1 不会单独造成死亡。
- 新增持久检定操作回执；模型回退、网络重试与 Bot 重载不得免费重投。
- 新增灵感点、一次性协助标记、伤势/状态和相关行动影响。
- 新增 4/6/8 格场景时钟、任务/线索/里程碑剧情账本和正式进度。
- 新增副本级 NPC、NPC 认知边界、模型生成登记门槛、单回合上限、待复核与疑似重复处理。
- 长期记忆并入群会话，新增锁定、置顶、可见范围、失效、冲突和事实替代。
- 新增世界/副本内容边界、上下文预算、玩家安全暂停与计时冻结。
- 重新定义关闭、完结和强制终止；完结会建立最终存档并永久只读，只能克隆续作。
- 新增从当前状态、指定存档或最终存档克隆副本分支。
- 群会话管理改为卡片，显示章节、目标、里程碑、阵容、NPC、记忆、存档、等待流程和恢复状态。
- 世界编辑器新增独立角色卡模板 JSON、结构/预算校验、默认恢复和建卡预览。
- 原生设置页新增 4 个标准备用供应商选择器；管理台保存后回读校验并显示配置修订。
- 新增模型链健康、连续失败、临时熔断和最近错误展示。
- 时间管理改为数值、单位与不限时；新增即时团、慢速群团、全手动预设；`0` 明确非法。
- 数据库升级至 Schema 4；新增迁移备份、永久归档、规则状态、动态 NPC、账本、时钟、记忆治理、灵感流水、配置修订与操作回执表。
- 自动化回归增至 70 项。

## v0.4.0-beta

> 内部测试版。`v1.0.0` 正式版号暂不启用，待功能与存档兼容性完成内部验证后再发布。

- 状态机升级为 `closed / preparing / running / paused / finished`；开启副本只进入准备大厅，开演才创建正式行动权。
- 新增持久化私聊建卡码、分步草稿、角色卡版本、审核、准备状态及角色运行状态。
- 新增世界人数建议、最低开演人数、强制人数上限和数据库原子席位占用。
- 玩家个人回合改为持久化 A/B/C/D 四选一；支持一次重整与受限演绎附言。
- 新增插件权威角色属性加值和 D20 两阶段检定。
- 新增关键节点集体多数表决、二轮决选、投票计时及个人回合挂起/恢复。
- 新增每个完整轮次一次的世界脉冲事件池，支持权重、轮次、冷却、唯一性及场景条件。
- 新增全局、世界模板、当前副本三级时间规则与持久计时器控制。
- 新增候补、暂离、代控授权、连续超时、安全退场、三级封禁、返场投票与返场剧情任务。
- 存档扩展为完整工作流快照，覆盖参与状态、角色运行状态、未完成选项、投票、计时、权限和返场任务。
- 原生管理台新增阵容、角色审核、时间与流程、工作流、权限/封禁和返场管理。
- 数据库升级至 Schema 3；迁移前强制生成一致 SQLite 备份，旧运行副本安全转为暂停。
- 新增 v0.4 工作流、席位并发、权威属性、投票、代控、封禁、返场、工作流恢复和迁移备份测试。

## v0.3.0

- “长期记忆”由卡片网格改为紧凑列表，集中显示重要度、范围、类型、正文、标签、更新时间和操作。
- 新增有序备用聊天模型链；主模型超时、调用失败或结构始终无效时按顺序回退。
- 新增可选图片转述模型、转述提示词和单回合图片上限；只读取当前行动者带剧情前缀的消息图片。
- 图片先被转成受边界约束的客观文字，再进入叙事模型与时间线；未配置或转述失败时不提交该回合。
- 同一平台群支持多个独立命名副本，分别保存世界状态、玩家、轮次、事件、记忆与存档。
- `/酒馆 开启` 改为只显示副本选择，不再直接启动；使用 `/酒馆 开启 <副本标识>` 明确选择。
- 新增 `/酒馆 副本列表`（别名 `/酒馆 副本`），切换副本时自动暂停同群内原运行副本。
- 数据库 Schema 升级至 v2；旧版群会话自动迁移为以原世界命名的选中副本。
- 管理台新增模型下拉、备用顺序调整、图片模型设置和多副本建立/识别。
- 新增模型回退、图片转述、多副本隔离、旧库迁移和显式选择回归测试。

## v0.2.0

- 将剧情入口改为可配置的显式前缀，默认仅 `jg + 空格 + 内容` 能触发酒馆。
- 普通群聊在访问会话与记忆前直接旁路，不拦截、不调用模型、不写酒馆数据。
- 新增多人固定顺序、多人轮次与当前行动权令牌。
- 新增 `/酒馆 加入`、`退出`、`顺序`、`跳过` 与管理员 `/酒馆 下一位`。
- 非当前玩家的内容即时拒绝，原文不入库、不排队、不在上一位完成后延迟执行。
- 剧情、世界状态、记忆与行动权移交在同一事务内完成；裁定失败不丢失行动权。
- 玩家退出、停用或删除时自动维护队列；存档、读档与回滚同步保存多人轮次。
- 管理台新增触发前缀设置、当前行动者、多人轮次和玩家顺序调整。
- 叙事提示加入轮次上下文，禁止替其他玩家补行动。
- 新增精确前缀、普通消息旁路、抢话拒绝、轮转、跳过与队列维护回归测试。

## v0.1.2

- 将全部 `/酒馆 ...` 管理命令注册为 AstrBot 原生 `command_group` 子指令。
- 修复唤醒前缀被框架剥离后，`/酒馆 开启` 未被插件识别并落入普通 AI 对话的问题。
- 原生酒馆命令以更高优先级执行，并在处理开始时立即终止后续事件传播。
- 保留规范化消息兜底拦截，未知酒馆命令也不会进入普通 AI 对话。
- 存档名、读档名等命令参数支持保留完整尾随文本。
- 新增原生指令注册、真实事件 ID、前缀剥离和独占拦截回归测试。

## v0.1.1

- 修复授权管理员无法从未绑定群首次执行 `/酒馆 开启` 的初始化死锁。
- 首次开馆会从真实事件自动识别平台实例 ID 与群 ID，并将当前群写入允许群列表。
- 未配置管理员 ID 时返回明确初始化提示；未授权用户仍无法绑定或建立会话。
- WebUI 手动建立群会话后同步加入允许群列表。
- 修复“建立群会话”弹窗的取消与关闭按钮误触发表单提交。
- 手动 ID 输入增加必填校验、字段释义与自动绑定引导。
- 新增自动绑定权限、空管理员配置和弹窗取消行为的回归测试。

## v0.1.0

- 新增真实用户 ID 管理权限与群白名单。
- 新增关闭、运行、暂停和维护状态机。
- 新增世界包、常驻角色卡、玩家资料与初始示例世界。
- 新增严格结构化叙事和插件权威 D20 两阶段检定。
- 新增状态字段白名单、输入边界与提示注入隔离。
- 新增 SQLite 时间线、长期记忆、命名存档和审计。
- 新增每回合回滚点、定期检查点、安全快照和分支恢复。
- 新增完整 JSON 备份、插入式安全合并与覆盖恢复。
- 新增 AstrBot Plugin Pages 原生管理控制台。
- 新增深浅主题、移动端布局、SSE 刷新和配置页面。
- 新增核心、数据库、引擎和静态 UI 自动化测试。
