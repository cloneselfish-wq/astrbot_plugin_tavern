# v0.12.0 测试报告

## 测试范围

覆盖新安装、数据库迁移与备份、旧存档和世界快照、世界协议 v2—v5、角色建卡、回合、
选项、投票、检定、计时器、托管、人工 DM、关系、背包、经济、NPC、Token、通知发件箱、
WebAPI、扩展注册表、静态 WebUI、版本同步、JSON、Python 和 JavaScript 语法。

## v0.12.0 专项

- 验证 `/extensions` 路由绑定的是可调用异步处理器，而不是扩展注册表实例。
- 验证内置 `d20` 扩展可以从真实路由响应中读取。
- 注入一个 `__str__` 抛异常的扩展名称，确认正常扩展仍返回、异常仅进入 `errors`，响应不崩溃。
- 静态验证页面使用 `Promise.allSettled`，并包含加载、空、失败、部分失败、重试和减少动态效果状态。
- 验证 `v0.12.0` 在常量、元数据、帮助文本、WebUI 静态资源、模板和备份文件名中一致。

## 自动化结果

- Python 全量回归：`280 passed, 3 skipped`，耗时 220.14 秒。
- Python 全仓编译：通过。
- `pages/console/app.js` JavaScript 语法检查：通过。
- 模板和内置世界 JSON：12 个文件全部解析通过。
- 版本同步检查：通过。
- 扩展点专项测试：3 项通过，并包含在上述 280 项中。

3 项跳过均为既有兼容边界：1 项是已声明不支持的 Schema 1—7/v1 迁移路径；2 项是已被
职业预设替代的旧四属性默认模板测试。通用手动属性仍由自定义世界包夹具覆盖，没有以跳过
掩盖 v0.12.0 当前功能。

## 浏览器检查

- 应用内浏览器成功加载控制器外壳、导航、样式和模块脚本。
- 独立静态打开时出现“AstrBot 管理台桥接对象不可用”，这是脱离 AstrBot 宿主的预期行为。
- Playwright Chromium 运行时下载因测试环境网络在 0% 阶段超时，因此未重复执行原有完整
  模拟桥浏览器脚本；前端状态由静态测试覆盖，后端接口由真实路由测试覆盖。

## 安装包检查

安装包与源码包 ZIP CRC 均通过；安装包包含 115 个文件，源码包包含 149 个文件。安装包使用
单一 `astrbot_plugin_tavern/` 根目录，不含 `__pycache__`、`.pyc` 或越界条目；包内元数据版本
为 `0.12.0`，解压后 Python 编译通过，CHANGELOG 不再包含独立 A1—A24 标题。
